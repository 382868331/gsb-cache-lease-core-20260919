# 完整首轮任务

请从零实现“分代缓存租约与过期状态机”，用于并发库 / 缓存一致性中的本地可复用模块。

1. 从零实现有容量上限的内存缓存协调库，键和值为字符串。时间由调用者传入单调 int64 tick，不读取系统时钟；TTL为正整数，now>=expiresAt即过期，时间回退和加法溢出返回错误。
2. Acquire(key,now) 返回 Hit(value)、Leader(lease) 或 Pending(leaseID)；同键同代只产生一个在途租约。库不调用加载器、不阻塞等待、不发网络请求。Complete(lease,value,ttl,now)与Fail(lease,now)由外部显式调用。
3. Invalidate(key,now)使缓存及在途租约失效；旧 Complete 必须返回 stale，绝不能覆盖更新值。租约 ID 全局不复用，Clear 同样使全部旧租约失效。Fail 只释放匹配租约，不删除后续代的数据。
4. 容量按已缓存键数计算，LRU只由成功Hit和成功Complete更新，同刻用内部递增序号定序；过期项先剔除再驱逐。在途租约另设固定上限，达到上限时拒绝新Leader但已有Pending仍可查询。
5. 所有API需并发安全且行为可线性化；公开只读状态快照，用显式同步屏障构造失效/完成竞态，不用 sleep 猜时序。用顺序参考状态机验证生成操作序列，Go race仅环境可用时运行，不为race安装外部编译器。

验收重点：恰好过期、重复Complete、旧租约回填、Clear竞态、容量1驱逐、在途上限、时钟回退、溢出原子拒绝。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
