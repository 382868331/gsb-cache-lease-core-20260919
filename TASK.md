# 完整首轮任务

缓存加载协调器需要防止失效之后在途旧加载结果重新填入缓存。请从零实现“分代缓存租约与过期状态机”。

1. 实现字符串键值内存缓存协调库，缓存容量及在途上限均为正整数。时间是调用者传入的非负int64 tick；所有成功修改/Acquire在线性化顺序上维护一个全局now高水位，小于高水位则拒绝，允许相等。
2. Acquire(key,now)返回Hit(value)、Leader(lease)或Pending(leaseID)，同键同代仅一个租约。Complete(lease,value,ttl,now)写入，ttl>0且now+ttl不溢出，now>=expiresAt即过期。库不调用加载器，不阻塞、不联网。
3. Invalidate(key,now)、Clear(now)使相应缓存和租约失效；旧Complete/Fail返回stale，不能影响新代。租约不会自动到期，由调用者Complete/Fail/Invalidate结束；租约ID不复用，计数耗尽明确拒绝。
4. 容量只计缓存项；成功Hit/Complete更新LRU，序号定序，写入时先剔除过期项再驱逐。达到在途上限拒绝新Leader，现有Pending仍可查。任意错误（包括stale/时间回退/超限）不改变数据、LRU、租约或时钟。
5. API并发安全可线性化，只读快照不推进时钟；可用一把锁，无需无锁算法。用同步屏障验证失效/完成竞态和顺序参考模型，不用sleep猜时序；race仅环境可用时执行，不为它安装外部编译器。

验收重点：同tick、时间回退、恰好过期、旧租约回填、Clear竞态、LRU容量1、在途上限、错误不推进时钟。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
