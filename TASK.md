# 完整首轮任务

请实现缓存租约的状态管理库。缓存项有容量、过期时间和分代信息，调用方可能在持有旧租约时遇到更新或清空；这些操作需要有明确且一致的结果。

实现字符串键值内存缓存协调库，缓存容量及在途上限均为正整数。时间是调用者传入的非负int64 tick；所有成功修改/Acquire在线性化顺序上维护一个全局now高水位，小于高水位则拒绝，允许相等。

Acquire(key,now)返回Hit(value)、Leader(lease)或Pending(leaseID)，同键同代仅一个租约。Complete(lease,value,ttl,now)写入，ttl>0且now+ttl不溢出，now>=expiresAt即过期。库不调用加载器，不阻塞、不联网。

Invalidate(key,now)、Clear(now)使相应缓存和租约失效；旧Complete/Fail返回stale，不能影响新代。租约不会自动到期，由调用者Complete/Fail/Invalidate结束；租约ID不复用，计数耗尽明确拒绝。

容量只计缓存项；成功Hit/Complete更新LRU，序号定序，写入时先剔除过期项再驱逐。达到在途上限拒绝新Leader，现有Pending仍可查。任意错误（包括stale/时间回退/超限）不改变数据、LRU、租约或时钟。

API并发安全可线性化，只读快照不推进时钟；可用一把锁，无需无锁算法。用同步屏障验证失效/完成竞态和顺序参考模型，不用sleep猜时序；race仅环境可用时执行，不为它安装外部编译器。

测试里请覆盖这些情况：同tick、时间回退、恰好过期、旧租约回填、Clear竞态、LRU容量1、在途上限、错误不推进时钟。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
